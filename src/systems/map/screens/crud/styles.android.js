import {StyleSheet} from "react-native";
import ColorPalette from "../../../../assets/color-palette";

export default styles = StyleSheet.create({
    gridView: {
        paddingTop: 25,
        flex: 1
    },
    addButton: {
        backgroundColor: ColorPalette.AddButton,
        borderRadius: 8
    }
});